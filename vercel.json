{
  "buildCommand": "npm run build:vercel",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "npm install",
  "functions": {
    "api/**/*.py": {
      "excludeFiles": "{.next,*.cache,node_modules,public,app,dist,build,src,components,EXTERNALPROYECTS}/**",
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/api/tempo",
      "destination": "/api/tempo.py"
    }
  ]
}