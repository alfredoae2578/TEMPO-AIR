# Flask web framework and extensions (core only)
Flask==2.3.3
Flask-CORS==4.0.0

# NASA Earth data access (optimized version)
earthaccess==0.8.2

# Minimal scientific computing stack
numpy==1.24.4
xarray==2023.5.0
netCDF4==1.6.4
h5netcdf==1.2.0

# Remove heavy dependencies:
# - scipy (not needed for basic operations)
# - pandas (xarray is sufficient)
# - dask (not needed for single coord queries)
# - python-dotenv (using os.environ directly)